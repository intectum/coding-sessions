linux: = import("os/linux")

exit: proc(code: u8) = linux.exit(i64(code))

time: proc() -> i64 =
{
  timeval: linux.timeval_t
  linux.gettimeofday(^timeval)
  return timeval.seconds * 1000000 + timeval.micro_seconds
}
