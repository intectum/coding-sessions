os: = import("core/os")
strings: = import("core/strings")

assert: proc(assertion: bool, message: string) =
{
  if !assertion
  {
    strings.print("Assertion failed: ")
    strings.println(message)
    os.exit(1)
  }
}
