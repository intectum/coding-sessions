bugs/issues

- TEST EVERYTHING! including glsl
- negative tests (return errors from compile, fail if they are wrong messages, make for all error messages)
- glsl nilify
- glsl static vars
- glsl prevent calling in non-boomstick context
- bounds checking kernels
- calling procs from kernels
- check all TODOs
- switch only generate expression_node once
- auto-convert dynamic array sizes
- dynamically sized soa
- think through parsing messages
- deferred type resolution
- asm/glsl indenting etc.
- unify expression/assignment code, more efficient locations
- x86 asm review
- delete dynamic arrays/maps etc. in compiler after use
- removed _node from vars in compiler
- add expression to return in type checking

dev tools

- lsp
- tree sitter
- debug
- docs

nice-to-have features

- map type
- string format procs (include console/file read/write) - strings.format, os.print, os.println
- compiler performance
- ternary ? :
- composable structs
- procs with capture
- #partial switch
- compile-time execution
- OpenCL
- OpenGL rendering with bang kernels
- set affinity of threads in thread pool (sched_setaffinity?)
- _ var not saved
- multi-assign
- multi-return
- windows
- mac
- arm
- web
- android
- iOS

questions

- restrict generic param types?
- generic structs?
- variadics?
- const? by default for params? mutable?
- spread operator?
- default heap arena?
- byte pointers can be coerced to any other pointer type?
- default struct members? violates nil as default?
- nil coalesce? e.g. foo?.bar or foo ?? bar
- custom allocator for nodes in compiler?
- is coerce_type just upgrade and compare?
- switch slice range operator from : to ..?
- swap src/dest in copy?
- ref/deref procs?
- are atomic types necessary?
- lhs_expression/rhs_expression node types?
- switch to system-v calling convention?
- cint and cstring need to be explicitly imported? c.int and c.string?
- do not gen unreferenced vars? including static
